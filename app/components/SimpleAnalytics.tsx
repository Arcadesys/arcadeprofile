'use client';

import { Analytics } from '@vercel/analytics/react';

export default function SimpleAnalytics() {
  // This is a minimal wrapper that just renders the Analytics component
  // without any additional logic or error handling
  return <Analytics />;
} 